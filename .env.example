# Production API (requires valid SSL certificate on the server)
EXPO_PUBLIC_API_BASE_URL=https://iesdata.iliauni.edu.ge:2026/api
EXPO_PUBLIC_API_TOKEN=

# Dev only: Metro proxy target (self-signed SSL). Leave empty for default.
# EXPO_PUBLIC_API_PROXY_TARGET=https://iesdata.iliauni.edu.ge:2026

# Dev only: override proxy URL if auto-detection fails (same Wi‑Fi as dev machine)
# EXPO_PUBLIC_DEV_API_BASE_URL=http://192.168.1.10:8081/ies-api
